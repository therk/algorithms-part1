class Root {
  private int size;
  private int id;

  Root(int id, int size) {
    this.id = id;
    this.size = size;
  }

  int getId() {
    return id;
  }

  int getSize() {
    return size;
  }
}